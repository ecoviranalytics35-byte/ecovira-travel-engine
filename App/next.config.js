/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: The lockfile warning is informational and doesn't affect builds
  // If you want to silence it, remove one of the package-lock.json files
  // (either root or App/package-lock.json, depending on your project structure)
};
module.exports = nextConfig;